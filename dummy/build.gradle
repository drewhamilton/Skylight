apply plugin: 'java-library'
apply plugin: 'org.jetbrains.kotlin.jvm'
apply plugin: 'org.jetbrains.kotlin.kapt'

ext {
    artifactName = 'skylight-dummy'
    publishedDescription = 'A dummy implementation of Skylight that always returns the same skylight events.'
}
apply from: '../publish.gradle'

dependencies {
    api project(':skylight')

    implementation deps.javaxInject
    implementation deps.kotlinStdLib

    testImplementation deps.junit
    testImplementation deps.dagger
    kaptTest deps.daggerCompiler
}

sourceCompatibility = '1.8'
targetCompatibility = '1.8'
